# TailwindCSS Quickstarter

## Installation

1. Clone Repository
2. Run the `npm install` command to install the dependencies
3. Run `browser-sync start --server --files "*.html, css/*.css"` to start the dev server